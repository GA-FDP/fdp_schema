[workspace]
name = "fdp_schema"
channels = ["conda-forge"]
platforms = ["linux-64"]

[dependencies]
python = "3.11.*"
pydantic = ">=2"
pyyaml = "*"
pytest = "*"
versioneer = "*"

[pypi-dependencies]
fdp_schema = { path = ".", editable = true }

[tasks]
test = "pytest -v"
